# First Story

A roughly 30-second single-character reference story. It reuses the bundled
Alex character, starter room, and plant assets. Render it with:

```bash
al-studio render projects/first-story/project.json projects/first-story/script.json --output output/first-story --verbose
```
